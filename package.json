{
  "name": "pingtest",
  "private": true,
  "scripts": {
    "dev": "cd client && vite --host",
    "server": "tsx server/src/index.ts",
    "build": "cd client && vite build"
  },
  "workspaces": [
    "shared",
    "server",
    "client",
    "probe-agent"
  ]
}
